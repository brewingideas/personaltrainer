<h1>Sign up to personal trainer and see the impovements in your workout!</h1>

<%= simple_form_for @user do |f| %>
	<%= f.input :username %>
	<%= f.input :email %>
	<%= f.input :password %>
	<%= f.input :password_confirmation %>
	<%= f.button :submit, "Sign up" %>
<% end %>